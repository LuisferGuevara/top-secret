import { Flex, Heading } from '@chakra-ui/react';

const Support = () => {
  return (
    <Flex>
      <Heading flex={1} size={'lg'}>
        Soporte
      </Heading>
    </Flex>
  );
};

export default Support;
