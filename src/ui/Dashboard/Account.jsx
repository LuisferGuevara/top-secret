import { Flex, Heading } from '@chakra-ui/react';

const Account = () => {
  return (
    <Flex>
      <Heading flex={1} size={'lg'}>
        Mi Perfil
      </Heading>
    </Flex>
  );
};

export default Account;
