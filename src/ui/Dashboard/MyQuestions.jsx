import { Flex, Heading } from '@chakra-ui/react';

const MyQuestions = () => {
  return (
    <Flex>
      <Heading flex={1} size={'lg'}>
        Mis Preguntas
      </Heading>
    </Flex>
  );
};

export default MyQuestions;
