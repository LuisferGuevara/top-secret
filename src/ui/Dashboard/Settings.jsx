import { Flex, Heading } from '@chakra-ui/react';

const Settings = () => {
  return (
    <Flex>
      <Heading flex={1} size={'lg'}>
        Ajustes
      </Heading>
    </Flex>
  );
};

export default Settings;
