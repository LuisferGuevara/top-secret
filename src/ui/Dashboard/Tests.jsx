import { Flex, Heading } from '@chakra-ui/react';

const Tests = () => {
  return (
    <Flex>
      <Heading flex={1} size={'lg'}>
        Pantalla de Testing
      </Heading>
    </Flex>
  );
};

export default Tests;
